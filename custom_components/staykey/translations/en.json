{
  "title": "StayKey",
  "config": {
    "step": {
      "user": {
        "title": "Configure StayKey",
        "description": "Enter the gateway token from your Staykey dashboard to enable the gateway connection. Alternatively, provide a webhook URL for legacy mode.",
        "data": {
          "gateway_token": "Gateway Token",
          "gateway_url": "Gateway URL",
          "endpoint_url": "Webhook URL (legacy)"
        }
      }
    },
    "error": {
      "must_provide_token_or_url": "You must provide either a gateway token or a webhook URL."
    },
    "abort": {
      "already_configured": "This integration is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "StayKey Options",
        "data": {
          "gateway_token": "Gateway Token",
          "gateway_url": "Gateway URL",
          "forward_all_notifications": "Forward all Z-Wave notifications (not only user code)",
          "verify_ssl": "Verify TLS certificates",
          "timeout": "Webhook timeout (seconds)",
          "endpoint_url": "Webhook URL (legacy)"
        }
      }
    }
  }
}
