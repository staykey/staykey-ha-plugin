{
  "title": "Staykey",
  "config": {
    "step": {
      "user": {
        "title": "Configure Staykey",
        "description": "Enter the gateway token from your Staykey dashboard (Properties > Integrations > Home Assistant) to connect this Home Assistant instance to Staykey.",
        "data": {
          "gateway_token": "Gateway Token",
          "gateway_url": "Gateway URL"
        }
      }
    },
    "error": {
      "gateway_token_required": "A gateway token is required. Copy it from your Staykey dashboard under Properties > Integrations > Home Assistant."
    },
    "abort": {
      "already_configured": "This integration is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Staykey Options",
        "data": {
          "gateway_token": "Gateway Token",
          "gateway_url": "Gateway URL",
          "verify_ssl": "Verify TLS certificates",
          "timeout": "Webhook timeout (seconds)",
          "endpoint_url": "Webhook URL (deprecated - migrate to a gateway token)"
        }
      }
    }
  },
  "issues": {
    "webhook_mode_deprecated": {
      "title": "Staykey webhook mode is deprecated",
      "description": "This Staykey integration is configured to forward events over the legacy webhook, which Staykey has retired — lock events sent this way no longer reach Staykey. Copy the gateway token from your Staykey dashboard (Properties > Integrations > Home Assistant) and add it under Settings > Devices & Services > Staykey > Configure. The webhook URL can be removed once the gateway shows Connected."
    }
  }
}
